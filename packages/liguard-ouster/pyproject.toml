[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "liguard-ouster"
version = "1.0.0"
description = "Ouster LiDAR integration for LiGuard-Web"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "LiGuard-Web Team" }
]
keywords = ["liguard", "ouster", "lidar", "pointcloud"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]

dependencies = [
    "liguard-core>=1.0.0,<2.0.0",
    "ouster-sdk>=0.13.0",
    "numpy>=1.26",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov"]

[project.entry-points."liguard.plugins"]
ouster = "liguard_ouster:register"

[tool.hatch.build.targets.wheel]
packages = ["src/liguard_ouster"]
